import { SxProps, Theme } from "@mui/material";

export const styles: { [key: string]: SxProps<Theme> } = {
  avatarBadge: {
    display: 'flex',
    alignItems: 'center',
  },
  userNameOnBadge: {
    marginLeft: '20px'
  }
};
